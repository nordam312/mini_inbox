# Where the NestJS API is listening. Read on the server only - the browser
# never talks to the API directly.
API_URL=http://localhost:3001

# Which tenant the dashboard opens as. Override per request with ?tenant=...
DEFAULT_TENANT_ID=alsalam-motors
